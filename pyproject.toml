[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "rlsdc"
version = "0.1.0"
description = "Harness code for the RL self-driving-car learning project (not a published package)."
requires-python = ">=3.11"

[tool.setuptools]
packages = ["rlsdc"]
